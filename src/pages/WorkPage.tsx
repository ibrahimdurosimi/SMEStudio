import { Work } from '../components/Work';

export function WorkPage() {
  return (
    <main id="top" style={{ paddingTop: '80px', background: 'var(--paper)', minHeight: '100vh' }}>
      <Work />
    </main>
  );
}
